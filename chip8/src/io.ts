import * as vm from "./vm";

/**
 * A concrete implementation of vm.IO
 */
export class IO implements vm.IO {
  clearDisplay() {
    TODO();
  }
}

function TODO(): never {
  throw new Error("TODO (IO method)");
}
